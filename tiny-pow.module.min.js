const a=t=>[...new Uint8Array(t)].map(t=>t.toString(16).padStart(2,"0")).join("");export default async(t,e=3)=>{let n,r=0;for(;a(await crypto.subtle.digest("SHA-256",(new TextEncoder).encode(t+n))).slice(0,e)!=="0".repeat(e);)if(n=a(crypto.getRandomValues(new Uint8Array(8))),999999<r++)return;return n}